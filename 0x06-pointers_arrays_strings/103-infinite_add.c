#include "main.h"
 /**
 * main - infinity number.
 * Return: 0 always
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r);
{
	char *n = i;
	char *n1[100];
	char *n2[10];
/**
 *@n1: Number one.
 * @n2: value for second number.
 * @r: beffer funciton to store result.
 * @size_r: to store buffer size.
 */
	return (0);
}
